#!/bin/bash
# Nudge: delegate implementation work to the orchestrator

echo "=== Workflow Reminder ==="
echo "For features, fixes, or UI changes → delegate to @orchestrator"
echo "It manages the full pipeline: architect → coder → tester → reviewer → design-qa"
echo "For quick questions or git ops → handle directly"
echo "=== End Reminder ==="
